<h1>Your series</h1>

<%= link_to "Add new series", new_series_path, class: 'button' %>

<ul>
  <% @series.all do |series| %>
    <li><%= link_to series.name, series_path(series) %></li>
  <% end %>
</ul>
